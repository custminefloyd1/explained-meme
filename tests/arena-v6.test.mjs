import {readFileSync} from 'node:fs';
const html=readFileSync(new URL('../explained meme/index.html',import.meta.url),'utf8');
const start=html.indexOf('/* Battle Arena v6:');
const end=html.indexOf('\nfunction Ti(){',start);
if(start<0||end<0)throw Error('Component missing');
const code=html.slice(start,end);
for(const m of html.matchAll(/<script\b[^>]*>([\s\S]*?)<\/script>/g))new Function(m[1]);

const run=new Function('code',"return (async()=>{\nasync function testMode(local=false,fail=false,ten=false,consensus=false){\n let stateIndex=0,refIndex=0;const states=[],refs=[],effects=[],writes=new Map(),calls=[];\n const sample=Array.from({length:6},(_,i)=>({id:\"m\"+i,title:\"Meme \"+i,image_url:\"https://example.com/\"+i+\".png\",elo:1200,wins:0,losses:0,rank:i+1,created_at:new Date().toISOString()}));\n const R={useState(v){const i=stateIndex++;if(!(i in states))states[i]=typeof v===\"function\"?v():v;return[states[i],next=>states[i]=typeof next===\"function\"?next(states[i]):next]},\n useRef(v){const i=refIndex++;return refs[i]||(refs[i]={current:v})},useEffect(fn){effects.push(fn)},\n createElement(type,props,...children){return {type,props:props||{},children}},Fragment:\"Fragment\"};\n function walk(tree,match){const found=[];function visit(n){if(!n||typeof n!==\"object\")return;if(Array.isArray(n)){n.forEach(visit);return}if(match(n))found.push(n);n.children?.forEach(visit)}visit(tree);return found}\n const fetch=async(url,options)=>{\n calls.push({url,options});if(url.includes(\"/signup\"))return {ok:true,json:async()=>({access_token:\"test\",expires_in:3600})};\n if(url.includes(\"arena_state_v7\"))return {ok:true,json:async()=>({pool:sample,closes_at:new Date(Date.now()+86400000).toISOString(),week_start:new Date().toISOString(),server_now:new Date().toISOString(),new_today:6,new_since:2,eligible_count:6})};\n if(url.includes(\"arena_vote_v7\")){\n if(fail)return {ok:false,json:async()=>({})};\n const vote=JSON.parse(options.body);\n return {ok:true,json:async()=>({rows:sample.map(m=>({...m,elo:m.id===vote.p_winner?1216:1200})),before_rank:3,after_rank:2,delta:16,agreement:consensus?68:null,sample:consensus?25:0,majority:consensus?true:null})};\n }\n throw Error(\"Unexpected network call \"+url)};\n const make=new Function(\"F\",\"fetch\",\"kn\",\"be\",\"localStorage\",\"location\",\"window\",\"document\",\"crypto\",\"setInterval\",\"clearInterval\",\"setTimeout\",\"clearTimeout\",code+\";return ArenaV6\");\n const component=make(R,fetch,\"https://mock.supabase.co\",\"public\",{getItem:k=>writes.get(k)||null,setItem:(k,v)=>writes.set(k,v)},{protocol:local?\"file:\":\"https:\",href:\"https://example.com\"},{addEventListener(){},removeEventListener(){}},{activeElement:null},{randomUUID:()=> \"00000000-0000-4000-8000-000000000001\"},()=>1,()=>{},()=>1,()=>{});\n function render(){stateIndex=0;refIndex=0;effects.length=0;return component()}\n render();effects[0]();for(let i=0;i<15;i++)await Promise.resolve();\n let tree=render(),initial=walk(tree,n=>n.props.className===\"av-image-button\");\n if(initial.length!==2)throw Error(\"Missing matchup\");\n for(let i=0;i<(ten?10:1);i++){\n   walk(tree,n=>n.type===\"img\"&&n.props.className===\"av-meme\").forEach(n=>n.props.onLoad());\n   tree=render();const picks=walk(tree,n=>n.props.className===\"av-image-button\");\n   if(picks.length!==2)throw Error(\"Matchups exhausted early at \"+i);\n   const before=picks[0].children[0].props.src+\"|\"+picks[1].children[0].props.src;\n   const action=picks[0].props.onClick();const duplicate=picks[0].props.onClick();await Promise.all([action,duplicate]);tree=render();\n   if(!fail&&!ten){const after=walk(tree,n=>n.props.className===\"av-image-button\");if(after.length!==2)throw Error(\"Next matchup missing\");const key=after[0].children[0].props.src+\"|\"+after[1].children[0].props.src;if(key===before)throw Error(\"Same pair after vote\")}\n }\n const tests=[\n {test:(local?\"demo\":\"live\")+(fail?\" failure\":\" success\")+\" double-submit guard\",pass:local||calls.filter(x=>x.url.includes(\"arena_vote_v7\")).length===(ten?10:1)},\n {test:\"no direct score PATCH\",pass:!calls.some(x=>x.options.method===\"PATCH\")},\n {test:fail?\"failed vote shows error\":\"feedback visible\",pass:walk(tree,n=>n.props.className===(fail?\"av-error\":\"av-feedback\")).length===1},\n {test:\"no next-confirmation control\",pass:walk(tree,n=>n.type===\"button\").every(n=>!n.children.some(x=>typeof x===\"string\"&&x.includes(\"Next battle\")))}\n ];\n if(local)tests.push({test:\"demo makes no network requests\",pass:calls.length===0},{test:\"demo writes no persistent data\",pass:writes.size===0});\n if(!local&&!fail)tests.push({test:consensus?\"recorded consensus displayed\":\"low sample never invents agreement\",pass:JSON.stringify(tree).includes(consensus?\"68% of earlier votes agreed with you\":\"not enough votes for crowd agreement.\")});\n if(fail)tests.push({test:\"failed vote doesn't increment daily total\",pass:!writes.has(\"arena-v7-journal\")});\n if(ten)tests.push({test:\"tenth pick shows result card\",pass:walk(tree,n=>n.props.className===\"av-complete\").length===1},{test:\"result label matches mode\",pass:JSON.stringify(tree).includes(local?\"DEMO COMPLETE\":\"DAILY TEN COMPLETE\")});\n return tests;\n}\nreturn [...await testMode(false,false),...await testMode(false,true),...await testMode(true,false),...await testMode(true,false,true),...await testMode(false,false,false,true),...await testMode(false,false,true,true)];\n})()");
const results=await run(code);
for(const r of results){console.log((r.pass?'PASS ':'FAIL ')+r.test);if(!r.pass)process.exitCode=1;}
